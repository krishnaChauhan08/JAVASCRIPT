// iterate  directly over the values of the elements,
//  rather than dealing with their keys or indices

let peoples = ["krishna", "huxn", "harkirat", "hitesh", "piyush"];
for (values of peoples) {
  console.log(values);
}

// execises
const array1 = ["a", "b", "c"];
for (const element of array1) {
  console.log(element);
}
